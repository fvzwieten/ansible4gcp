- name: test gcp_compute_windows_pwd
  hosts: localhost
  connection: local
  vars_files:
  - files/gcp_vars

  tasks:
  - name: run the new module
    gcp_compute_win_password:
      instance: "{{ vm_name }}"
      zone: "europe-west4-a"
      project: "ansible-workshop-244208"
      username: "ansible"
      email: "fvanzwie@redhat.com"
      credential_file: "{{ gcp_cred_file }}"
    register: testout

  - name: dump test output
    debug:
      msg: "{{ testout }}"
